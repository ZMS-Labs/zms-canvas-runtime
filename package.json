{
  "name": "@zms-labs/zms-canvas-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Archived container packaging for PenEcho 0.4.2.",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "penecho --config /config/config.env",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "penecho": "0.4.2"
  },
  "overrides": {
    "sharp": ">=0.35.0"
  }
}
